# 仕様書 — autonomous-content-bot

作成日: 2026-07-30
目的: AIが自律的にニッチを発見し、一次データに基づいたコンテンツ/ツールを
生成・公開し、広告収益で自身の運用費を賄う自律ループのプロトタイプ。
Polymarket自動売買ボット(観測→判断→実行→収支管理→生存チェック)と
同じ構造を、コンテンツ生成ドメインに適用したもの。

---

## 1. 全体アーキテクチャ

```
              ┌─────────────────┐
              │  main_loop.py    │  ← 10分おき等でcron実行される起点
              └────────┬─────────┘
                       │
 (1サイクルの流れ)
 1. 生存チェック(state.py)
 2. 再投資判断(reinvestment.py) — 儲かっているニッチを優先/打ち切りニッチを除外
 3. ニッチ探索(niche_scanner.py) — pytrends関心度スコア + 再投資判断の結果を反映
 4. 各ニッチについて:
      a. リサーチ(researcher.py) — 一次データ取得
      b. 判断(judge.py) — 生成する価値があるかAI評価
      c. ツール生成(tool_builder.py) or 記事生成(generator.py)
      d. 品質ゲート(quality_gate.py) — state.pyのcontent_corpusで永続類似度チェック
      e. 公開(publisher.py) — docs/への書き出し + GitHub Pagesへgit push
      f. state.pyにコンテンツ・ニッチ別コストを記録
 5. 収益取得(revenue_tracker.py) — manual CSV or AdSense API、ニッチ別に集計
 6. state.pyの残高・niche_statsを更新して保存
```

## 2. 生存ループの考え方(Polymarket版との対応)

| Polymarket版 | このプロジェクト |
|---|---|
| 数百の予測市場をスキャン | `niche_scanner.py` でニッチ候補を探索 |
| 本当の確率を計算 | `researcher.py` で一次データを取得 |
| 価格が間違ってる機会を探す | `quality_gate.py` で「公開する価値があるか」を判定 |
| いくら賭けるか決める | `main_loop.py` でツール生成/記事生成/スキップを判断 |
| 運用費を自分に抜く | `revenue_tracker.py` の収益からコストを差し引き、残高に反映 |
| 勝っているポジションに追加投資/負けを手仕舞う | `reinvestment.py` が `state.niche_stats` を見てニッチ別に再投資/打ち切りを判断 |
| 残高ゼロで消滅 | `state.py` の `check_survival()` が `alive: false` にして停止 |

## 3. モジュール別仕様と実装状況

### 3.1 `src/state.py` — 実装済み
- `data/state.json` に残高(`balance_usd`)・履歴(`history`)・公開済みslug一覧を永続化
- `log_event()` でコスト/収益/公開/停止イベントを記録し残高を増減
- `check_survival()` が `config.yaml: survival.min_balance_to_operate_usd` を下回ると `alive=false`

### 3.2 `src/niche_scanner.py` — 実装済み(需要×供給スコアリング)
- `pytrends`(非公式Google Trends API)で関連トレンドを取得し、失敗時は`config.yaml`の`seed_niches`にフォールバック
- `interest_over_time()`の平均値を需要シグナルとして候補をソート
- `SERPER_API_KEY`(`.env`)が設定されている場合、トレンドスコア上位候補(`MAX_SCARCITY_LOOKUPS_PER_CYCLE`件、既定6)についてSerper.dev経由で実際のGoogle検索結果件数を取得し、少ないほど高い希少性スコアとして需要スコアと合成する。これにより「検索需要はあるのに既存コンテンツが少ない」ギャップを実際のWeb検索結果に基づいて近似できる
- `SERPER_API_KEY`未設定時はpytrendsの関心度のみによるヒューリスティック近似にフォールバック(呼び出し失敗時も同様にフォールバック)
- `reinvestment.py`が算出した`priority_niches`(儲かっているニッチ)をスコアに関わらず優先し、`abandoned_niches`(打ち切り対象)は候補から除外
- **既知の制約**: Serper.devは従量課金のため呼び出し件数を1サイクルあたり上位候補のみに制限している。実アカウントでの検証は未実施(Serper.devキー未取得のため)

### 3.3 `src/researcher.py` — 為替・暗号資産のみ実装済み
- 為替: Frankfurter API(ECBデータ、無料・キー不要)→ `USD/EUR/GBP/AUD → JPY`
- 暗号資産: CoinGecko API(無料・キー不要)→ BTC/ETH/SOLの価格
- ニッチ名に暗号資産関連キーワードが含まれるかで`fx`/`crypto`を判定(`_looks_like_crypto`)
- `ResearchResult`:
  ```python
  niche: str
  has_unique_data: bool
  data_summary: Optional[str]   # 記事生成/品質ゲート用のテキスト
  sources: list
  kind: Optional[str]           # "fx" | "crypto" | None
  raw_data: Optional[dict]      # tool_builder.py が使う構造化データ
  ```
- データ取得失敗時(ネットワーク不可・API障害・レート制限429等)は`research_niche()`の`except Exception`が全て捕捉し`has_unique_data=False`を返すため、後続処理は安全側でスキップされる。CoinGecko無料枠のレート制限に引っかかっても、このサイクルをスキップするだけでクラッシュはしない
- **TODO**: 為替・暗号資産以外のニッチ(価格比較、統計データ等)は未対応。`kind`を増やすたびに`tool_builder.py`にも対応関数を追加する必要がある。これが2件のみだと、両方とも`reinvestment.py`で打ち切りになった場合や関連ワードが尽きた場合に探索対象が0件になりうる(`main_loop.py`が警告ログを出す)

### 3.4 `src/tool_builder.py` — 実装済み(為替・暗号資産のみ)
- AIにHTML/JSを直接書かせず、**Pythonの固定テンプレートに実データを埋め込む決定的な方式**
- `build_fx_converter_html()`: 金額入力+通貨選択+リアルタイム換算(クライアントサイドJS)
- `build_crypto_dashboard_html()`: 価格一覧テーブル+換算計算機
- `build_tool_html(research)`: `research.kind` で振り分け。未対応kindは`None`を返す
- モックデータでの動作確認済み(HTML構造・JS計算ロジック)

### 3.5 `src/generator.py` — 実装済み(フォールバック用)
- `tool_builder.py` が対応していないニッチ向けの、AIによるテキスト記事生成
- `ANTHROPIC_API_KEY` 未設定時はダミーテキストを返す(動作確認用)
- 独自データが無い場合は例外を投げて生成をブロックするガードあり

### 3.5b `src/judge.py` — 実装済み(判断ステップ)
- researcher.pyが独自データを取得できたニッチについて、実際に生成する価値があるかをAIに判定させる
- `ANTHROPIC_API_KEY`未設定時は「独自データがあれば常に許可」(旧来の暗黙挙動と互換)にフォールバック
- 設定時はClaudeにJSON形式(`{"worth_pursuing": bool, "reason": str}`)で判断させ、パース失敗時は安全側(許可)にフォールバック

### 3.6 `src/quality_gate.py` — 実装済み(永続化対応)
- 文字数チェック(`min_word_count`)
- 既存コンテンツとの類似度チェック(`difflib.SequenceMatcher`、`max_similarity_ratio`)
- `existing_texts`は`state.py`の`content_corpus`から`get_existing_texts()`で復元するため、サイクルをまたいでも類似度チェックが効く(旧: サイクル内メモリのみで次回起動時にリセットされていた問題を解消)
- **TODO**: `config.yaml`の`require_data_or_tool`フラグは現状コードから未参照のまま(未対応)

### 3.7 `src/publisher.py` — 実装済み(GitHub Pagesへの実デプロイ込み)
- `publish_tool()`: tool_builder が組み立てた完成済みHTMLをそのまま書き出す。**安定slug(ニッチ名のみ、タイムスタンプ無し)を使い、毎サイクル同じURLを上書き更新する**(為替/暗号資産レートは時間で古くなるため、鮮度を保つ設計。品質ゲートの対象外)
- `publish_article()`: テキスト記事を`<pre>`タグに包んで書き出す。記事は生成のたびに新しいタイムスタンプ付きslugを発行し、品質ゲート(類似度チェック)の対象になる
- `dry_run`モードでは実際のファイル書き込みをせずログのみ
- `live`モードでは書き出し後に`git add/commit/push`を自動実行し、GitHub Pages(`main`ブランチの`/docs`)へ反映する(`_git_publish()`)
- 公開先リポジトリ例: https://github.com/2t787kymfd-web/autonomous-content-bot (Pages URL: https://2t787kymfd-web.github.io/autonomous-content-bot/ )
- **TODO(AdSense承認後に必須)**: `docs/ads.txt`が未設置。AdSenseのパブリッシャーID(`pub-XXXXXXXXXXXXXXXX`)はアカウント承認後にAdSense管理画面で発行されるため、それまでは正しい内容のads.txtを作成できない。未設置のままだと広告需要元の一部が入札せず収益が本来より低くなる/気づかれないサイレントな失敗になりうるので、承認後は`echo "google.com, pub-XXXXXXXXXXXXXXXX, DIRECT, f08c47fec0942fa0" > docs/ads.txt`のように追加し、live公開すること

### 3.8 `src/revenue_tracker.py` — 実装済み(manual + AdSense、ニッチ別集計)
- `source: manual` の場合、`data/revenue_log.csv`(列: `date,amount_usd,niche`。niche列は空欄可)をニッチ別に集計
- `source: adsense_api` の場合、AdSense Management API(v2)にOAuth2リフレッシュトークンで接続し、`PAGE_PATH`次元のレポートを取得、`state.content_corpus`のslug→nicheマッピングでニッチ別に振り分ける
- `.env`の`ADSENSE_CLIENT_ID`/`ADSENSE_CLIENT_SECRET`/`ADSENSE_REFRESH_TOKEN`/`ADSENSE_ACCOUNT_ID`が未設定の場合は`RuntimeError`を投げ、main_loop側で捕捉して収益0のまま安全に継続する
- **安全装置**: レスポンスに`rows`キーが無い(パラメータ名ミスマッチの可能性)、または`content_corpus`に公開実績があるのに1件もniche紐付けできない(page_path形式のミスマッチの可能性)場合は、収益0を返すだけでなく明示的に警告ログを出す。「本当に収益0件」と「実装ミスで集計が壊れている」をログ上で区別できるようにするための対応
- **既知の制約**: AdSenseアカウントが未承認のため実データでのE2E検証は未実施。パラメータ名(dateRange/metrics/dimensions)は承認後にAPI仕様と突き合わせて要確認。上記の警告ログが出ないことをもって実データ検証の第一関門とする
- **TODO**: `affiliate_api`は対象外のまま未実装

### 3.9 `src/reinvestment.py` — 実装済み(再投資判断)
- `state.niche_stats`(ニッチ別の累積コスト・収益)を見て、累積損益が黒字のニッチを`priority_niches`(次サイクルで優先探索)に、`config.yaml: reinvestment.loss_threshold_usd`を下回ったニッチを`abandoned_niches`(以後除外)に振り分ける
- `reinvestment.min_cycles_before_judgement`回未満のニッチは判定を保留(サンプル数不足のため)
- **安全装置**: AdSense未承認・`revenue_log.csv`未記入などで収益データが一度も来ていないニッチ(`niche_stats.revenue_observed=False`)は、コストだけが積み上がって見かけ上赤字でも打ち切り判定から除外する(「収益が取れていない」と「儲からない」を混同しないため)。`reinvestment.enabled: false`で再投資判断そのものを丸ごと無効化することも可能

### 3.10 `src/main_loop.py` — 実装済み(全体オーケストレーション)
- 実行順: 生存チェック → 再投資判断 → ニッチ探索 → 各ニッチについて(リサーチ → 判断(judge) → ツール生成優先/記事生成フォールバック → 品質ゲート → 公開 → コンテンツをcontent_corpusに記録 → ニッチ別コスト計上)→ ニッチ別収益取得 → 固定費按分 → 状態保存
- `ESTIMATED_COST_PER_ARTICLE_GENERATION_USD`(0.05ドル)/`ESTIMATED_COST_PER_TOOL_REFRESH_USD`(0.005ドル)は概算値。実際のAPI利用料に応じて調整が必要
- 固定費按分は`state.py`の`apply_fixed_cost()`で実装済み。前回按分してからの実経過時間(日数)に応じて`survival.monthly_fixed_cost_usd`を日割りで残高から差し引く。cronの実行間隔にハードコードで依存しないため、間隔を変更しても自動的に正しく按分される

## 4. 設定ファイル `config.yaml` の項目

| キー | 内容 | 現状 |
|---|---|---|
| `mode` | `dry_run` / `live` | dry_run推奨(初期値) |
| `survival.starting_balance_usd` | 初期資金 | 50.0 |
| `survival.monthly_fixed_cost_usd` | 固定費(月額) | 4.0(DigitalOcean $4/moドロップレット実費。`apply_fixed_cost()`で日割り反映済み) |
| `survival.min_balance_to_operate_usd` | 生存しきい値 | 1.0 |
| `seed_niches` | 探索の種 | 為替・暗号資産の2件 |
| `niches_per_cycle` | 1サイクルの評価件数 | 3 |
| `quality_gate.*` | 品質ゲートのしきい値 | 上記3.6参照 |
| `llm.*` | 生成AIの設定 | anthropic / claude-sonnet-4-6 |
| `publish.output_dir` | 出力先 | `docs`(GitHub Pagesは`/`か`/docs`のみ対応) |
| `revenue.source` | 収益データソース | `manual` \| `adsense_api` |
| `reinvestment.enabled` | 再投資判断(優先/打ち切り)を有効にするか | true(収益データが無い間の誤打ち切りはrevenue_observedで別途防止済み) |
| `reinvestment.min_cycles_before_judgement` | 打ち切り判定までの猶予サイクル数(コスト基準) | 3 |
| `reinvestment.min_cycles_after_revenue` | 収益データが来始めてから打ち切り判定を許可するまでの猶予サイクル数 | 3 |
| `reinvestment.loss_threshold_usd` | この累積損益を下回ったら打ち切り | -1.00(生成コスト0.05ドル/回に対し余裕を持たせた値) |

## 5. 未実装・要対応のTODO一覧(優先度順の目安)

1. ~~**収益取得の実API接続**~~ — `revenue_tracker.py`にAdSense Management API接続を実装済み。ただしアカウント未承認のため実データ未検証。アフィリエイトAPIは対象外のまま
2. ~~**実デプロイ処理**~~ — `publisher.py`にGitHub Pagesへの`git push`を実装済み
3. ~~**固定費の按分をmain_loopで有効化**~~ — `state.py`の`apply_fixed_cost()`で実装済み。実経過時間ベースの日割り計算をテストで確認済み(1日経過で$4.0/30=$0.1333...が正確に課金されることを確認)
4. ~~**ニッチ探索の「機会スコア」化**~~ — `niche_scanner.py`にpytrends関心度(需要)+ Serper.dev検索結果件数(供給/既存コンテンツの多寡、`SERPER_API_KEY`設定時のみ)による機会スコアを実装済み。Serper.dev未設定時は関心度のみのヒューリスティック近似
5. **対応データ種別(kind)の追加**— 価格比較・統計データ等、`researcher.py`と`tool_builder.py`をペアで拡張(未対応)
6. **quality_gateとconfigの整合性**(`require_data_or_tool`が未使用、未対応)
7. ~~**既存コンテンツDBの永続化**~~ — `state.py`の`content_corpus`に永続化し、`get_existing_texts()`でサイクルをまたいで復元するよう修正済み
8. **判断ステップ(`judge.py`)・再投資判断(`reinvestment.py`)を追加**(新規実装。ニッチ別のコスト・収益を`state.niche_stats`に記録し、黒字ニッチへの優先探索・赤字ニッチの打ち切りをmain_loopに組み込み済み)
9. ~~**公開済みツールのデータが更新されない設計矛盾**~~ — `publish_tool()`の安定slug化+品質ゲート対象外化により解消。ツール更新コストも`ESTIMATED_COST_PER_TOOL_REFRESH_USD`として記事生成コストと分離し、実際のAPI利用実態(LLM呼び出し無し)に近づけた
10. **`docs/ads.txt`の設置**(AdSense承認後、パブリッシャーID発行後に対応。未対応)
11. **ニッチ探索対象が0件になる場合への対応**— `main_loop.py`が警告ログを出すようにしたが、根本対応(対応kindの追加)は5と同じくTODOのまま

## 6. 既知の制限・注意点

- **このサンドボックス環境はネットワーク接続がブロックされている**ため、`researcher.py`の外部API呼び出しは`403 Forbidden`で失敗する(コード自体は正常。実際のサーバー環境では動作するはず)
- Google検索/AdSenseの"scaled content abuse"、YouTubeの"inauthentic content"ポリシー(2026年時点)により、独自性のない量産コンテンツは収益化停止のリスクが高い。本プロジェクトは「独自データが無ければ生成しない」ガードを標準搭載しているが、これは目安であり規約遵守を保証するものではない
- 法的責任(税務・著作権・利用規約遵守)は運用者本人に帰属する

## 7. セットアップ手順(VSCode想定)

```bash
cd autonomous-content-bot
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
export ANTHROPIC_API_KEY="sk-ant-..."   # 記事生成フォールバックを使う場合
python3 -m src.main_loop
```

- まず `mode: dry_run` のまま数サイクル動かし、`site/`に出力されるHTML(または`dry_run`ログ)を確認
- 問題なければ `config.yaml` の `mode: live` に切り替え

## 8. ディレクトリ構成(現状)

```
autonomous-content-bot/
├── 仕様書.md          ← 本ファイル
├── README.md
├── config.yaml
├── requirements.txt
├── .env.example        # ANTHROPIC_API_KEY / ADSENSE_* の雛形
├── .gitignore
├── src/
│   ├── __init__.py
│   ├── state.py         # content_corpus / niche_stats を含む
│   ├── niche_scanner.py
│   ├── researcher.py
│   ├── judge.py          # 判断ステップ
│   ├── tool_builder.py
│   ├── generator.py
│   ├── quality_gate.py
│   ├── publisher.py      # GitHub Pagesへのgit push込み
│   ├── revenue_tracker.py# manual + AdSense
│   ├── reinvestment.py   # 再投資判断
│   └── main_loop.py
├── docs/                # 生成物の出力先(GitHub Pages配信元。index/about/privacy.html含む)
└── data/                # state.json, revenue_log.csv
```
